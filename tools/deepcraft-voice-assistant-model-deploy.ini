[tools]
llvm_dir =

[make_targets]
build = all
flash = deploy
clean = clean
all = all,deploy

[make_variables]
project = DEEPCRAFT_PROJECT
config = CONFIG
llvm = LLVM_DIR
serial = DEV_SERIAL_NUMBER
openocd = OPENOCD

[project]
repo_dir = ..
build_config = Debug

[board]

